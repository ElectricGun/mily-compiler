#include std/bulb.mily;

ref<double> @cell sus = 0;
ref<double> @cell1 var = sus;

var = 100;

void f(ref<double> x) {
    print(*x);
}

f(sus);
f(ref);
printflush(@message1);
-- prints 100100
-- because both references have the same value