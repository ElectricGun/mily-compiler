#include std/bulb.mily;

ptr<double> @cell2 sus = 0;
ptr<double> @cell2 var = sus;

var = 100;

void f(ptr<double> x) {
    print(*x);
}

f(sus);
f(var);
printflush(@message1);
-- prints 100100
-- because both references have the same value